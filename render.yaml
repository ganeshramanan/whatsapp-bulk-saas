services:
  # WhatsApp SaaS Web API & Worker Service
  - type: web
    name: grambi-whatsapp-saas
    runtime: node
    plan: free
    autoDeploy: false
    buildCommand: sed -i 's/provider = "sqlite"/provider = "postgresql"/g' prisma/schema.prisma && npm install && npx prisma generate && npm run build
    startCommand: sed -i 's/provider = "sqlite"/provider = "postgresql"/g' prisma/schema.prisma && npx prisma db push && node dist/index.js
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: grambi-postgres-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: grambi-redis
          property: connectionString

  # Managed Redis Queue Service
  - type: redis
    name: grambi-redis
    plan: free
    ipAllowList: []

databases:
  # Managed PostgreSQL Database
  - name: grambi-postgres-db
    plan: free
    ipAllowList: []


